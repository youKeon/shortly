apiVersion: 1

providers:
  - name: default
    orgId: 1
    folder: Shortly
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
